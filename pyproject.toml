[project]
name = "dota-autokey"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "keyboard",
    "pynput",
    "uvicorn[standard]",
]

[tool.uv]
python-preference = "only-managed"