[project]
name = "dota-autokey"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "httpx>=0.28.1",
    "keyboard",
    "pillow>=12.3.0",
    "pynput",
    "pyside6>=6.11.2",
    "pywin32>=312",
    "uvicorn[standard]",
]

[tool.uv]
python-preference = "only-managed"
