// Installed by install_configs.py; Dota runs this once at startup.
// F10 prints the camera position to the console (and to game/dota/console.log when Dota
// is launched with -condebug), which utils.Camera watches.
bind F10 "exec camera_position_query"
