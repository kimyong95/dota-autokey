// Installed by install_configs.py; Dota runs this once at startup.
// F10 prints the camera look-at position to the console (and to game/dota/console.log
// when Dota is launched with -condebug), which test-cam-position.py watches.
bind F10 "exec camera_position_query"
